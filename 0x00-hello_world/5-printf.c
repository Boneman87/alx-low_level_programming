#include <stdio.h>
/**
 * main is entry point
 * Success is achieved if program returns 0 to main function
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art, \n");
	return (0);
}
